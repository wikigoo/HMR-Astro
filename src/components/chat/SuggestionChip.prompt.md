**HMR SuggestionChip** — pre-written sample questions that seed the chat. Shown in a wrapping row above the composer on an empty conversation.

```jsx
<SuggestionChip onClick={ask}>Is an iPhone 13 without registry worth it?</SuggestionChip>
```

Props: `onClick`, `rtl`, `style`. Keep the text to one real question.
